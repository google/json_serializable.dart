# See https://github.com/dart-lang/mono_repo for details on this file
travis:
  sudo: required
  addons:
    chrome: stable
  branches:
    only:
    - master

merge_stages:
- analyzer_and_format
- ensure_build
- unit_test
